local consts = require("lua.play_consts")
local main = require("lua.play")
local skin = main.skin(consts.SKIN_TYPE_24K_DP, consts.RESOLUTION_HD)
if skin_config then
	return skin.body()
else
	return skin.header
end
